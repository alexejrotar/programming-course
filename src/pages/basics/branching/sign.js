function executeSign() {
  appendParagraph('sign', `Das Signum von 5 ist ${27}`);
  appendParagraph('sign', `Das Signum von -10 ist ${27}`);
  appendParagraph('sign', `Das Signum von 0 ist ${27}`);

}

executeSign();
