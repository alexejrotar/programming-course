{
  let a = 5;
  let absOfA = 0;
  let output = `<p>abs(${a}) = ${absOfA}</p>`;
  appendTo('abs-a', output);

  let b = -8;
  let absOfB = 0;
  output = `<p>abs(${b}) = ${absOfB}</p>`;
  appendTo('abs-b', output);
}
