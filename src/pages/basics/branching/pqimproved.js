{
  // the function should return [x1, x2] in case of two roots (e.g. [1, 3])
  // it should return x in case of one root (e.g. 5)
  // and it should return 'no roots' in case of no roots
  // your code here

  let roots1 = 'no roots'; // call the function here
  let output = `${roots1}`;
  appendTo('pqfun-improved-a', output);

  let roots2 = 0; // and here
  output = `${roots2}`;
  appendTo('pqfun-improved-b', output);
}
