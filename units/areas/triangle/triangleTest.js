triangleTest = new Test('triangleTest');

triangleTest.test('Triangle Area', () => {
	expected = 10;
	actual = triangleArea(5,4);
	assertSame(expected, actual);
});

triangleTest.test('Rectangular Triangle Height', () => {

});

triangleTest.test('Rectangular Triangle Area', () => {

});
