let hello = 'hello';

let index = 0;

appendParagraph('characters', hello[index]);
index++;
appendParagraph('characters', hello[index]);
index++;
appendParagraph('characters', hello[index]);
index++;
appendParagraph('characters', hello[index]);
index++;
appendParagraph('characters', hello[index]);
