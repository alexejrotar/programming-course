let hello = 'hello';

appendParagraph('characters', hello);
appendParagraph('characters', hello);
appendParagraph('characters', hello);
appendParagraph('characters', hello);
appendParagraph('characters', hello);
