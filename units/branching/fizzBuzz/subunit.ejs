<p>
Schreibe eine Funktion <code>fizzBuzz</code> in der Datei
<code>fizzBuzz.js</code>, die folgendes tut: Sie beginnt mit 0 und zählt mit
jedem Aufruf hoch (ähnlich wie der frühere Counter).  Allerdings gibt sie nicht
einfach die aktuelle Zahl zurück, sondern antwortet entweder "fizz", falls die
aktuelle Zahl durch 3 teilbar ist, "buzz", falls die aktuelle Zahl durch 5
teilbar ist, und "fizzbuzz", falls die aktuelle Zahl durch 15 teilbar ist.
</p>
<div id="fizzBuzz" class="output"></div>
