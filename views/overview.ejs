<!doctype html>
<html>
  <head>
    <title><%= title %></title>
    <%- include('views/head') %>
  </head>
  <body>
    <div class="container">
      <h1><%= title %></h1>
      <nav>
        <ol>
          <%_ links.forEach(function(link) { %>
          <li>
            <a href="<%= link.identifier %>/unit.html"><%= link.text %></a>
          </li>
          <%_ }); %>
        </ol>
      </nav>
  </body>
</html>
